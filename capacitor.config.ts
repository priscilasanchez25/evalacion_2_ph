import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ev.pri.san',
  appName: 'eva_priscila',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
